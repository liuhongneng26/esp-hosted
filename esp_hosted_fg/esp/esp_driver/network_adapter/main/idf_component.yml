dependencies:
  espressif/iperf:
    version: "*"
    rules:
      - if: target not in ["esp32c2"]
  espressif/iperf-cmd:
    version: "~0.1.1"
    rules:
      - if: target not in ["esp32c2"]
  esp-qa/wifi-cmd:
    version: "~0.1.0"
    rules:
      - if: target not in ["esp32c2"]
  esp-qa/ping-cmd:
    version: "~0.0.1"
    rules:
      - if: target not in ["esp32c2"]
